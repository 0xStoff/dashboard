packages:
  - apps/*
  - packages/*

autoInstallPeers: false

# The legacy backend and frontend intentionally retain their existing lockfiles
# and commands until the migration plan reaches the cutover phase.
